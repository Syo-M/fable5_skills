# Trigger evaluation — rules under test: v1.9.1 (working tree; 3 run(s)/prompt)

- Model: claude-sonnet-4-6 · max-turns 3 · fixture: Vite+React SPA
- Result: **0/3 prompts fully passed** (0 flaky, 3 failed)
- Method: headless `claude -p` in a disposable project installed via install.sh; activations measured
  by hooks (PostToolUse Skill/Task, SubagentStart, InstructionsLoaded) — observed, not self-reported.
- Caveat: release-grade run count.

| id | lang | rate | expected | observed (union) | notes |
|---|---|---|---|---|---|
| security-form-jp | ja | 0/3 | skills:frontend-security | react-patterns, css-modules, rule:styling |  |
| chart-en | en | 0/3 | skills:data-viz | react-patterns, rule:server-boundaries, rule:sensitive-config, rule:styling |  |
| react-state-jp | ja | 0/3 | skills:react-patterns | rule:server-boundaries |  |

> **CONCLUSION**: all three remain 0/3 AFTER description-level fixes (react-patterns in-body
> pointers, data-viz description widening) — the same lever that fixed `chart-jp` (0/3 → 2/3 in
> the main series). These are structural, not anchor gaps: `react-state-jp`/`security-form-jp`
> spend the 3-turn horizon in investigation/UI phases before the code moment where load-first
> bites; `chart-en` routes "table" to react-patterns. Floor analysis: in every failing run either
> `react-patterns` loaded (a rulebook governs the work) or path rules loaded, and the CLAUDE.md
> non-negotiables (always resident) carry the zod-validation floor for the form case. Next lever
> is a max-turns sensitivity experiment, tracked in eval/README Backlog.

> **PROVENANCE (post-hoc)**: SUBSET run — `--only security-form-jp,chart-en,react-state-jp`
> (3 of 29 prompts), NOT a full-series result; run against the v2.0.0-candidate tree after the
> description fixes. Precision correction: "chart-jp 0/3 → 2/3" should read "0 passes across
> prior valid measurements (0/1 in the post-fix re-run + 0/1 in run 3's degraded region) → 2/3
> in the release-grade series" — the pre-fix denominator was never a clean 3-run series.
