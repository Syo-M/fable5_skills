# Outcome-quality bench — rules under test: v3.0.3-dirty (working tree)

- Mode: planted-vulnerability recall (security review) + fix smoke · runs: 3/3 valid
- Model: session default · avg review time: 197s
- **Recall: 18/18 vuln-detections across runs** · **Decoy false positives: 1/6**
- Grading: transparent file+keyword matching vs expected-findings.json (an LLM-free grader — misses
  paraphrases; treat as a floor, not a ceiling). Fix smoke: fix-applied: NO / typecheck: FAIL

| planted vuln | detected |
|---|---|
| V1-no-validation (src/api/users/route.ts) | 3/3 |
| V2-webhook-no-signature (src/api/webhook/route.ts) | 3/3 |
| V3-xss-innerhtml (src/components/Comment.tsx) | 3/3 |
| V4-ssrf (src/api/preview/route.ts) | 3/3 |
| V5-idor (src/api/users/route.ts) | 3/3 |
| V6-hardcoded-secret (src/lib/config.ts) | 3/3 |

| benign decoy | falsely flagged |
|---|---|
| D1-sanitized-html (src/components/SafeHtml.tsx) | 1/3 |
| D2-benign-helper (src/lib/authorize.ts) | 0/3 |

> **PROVENANCE + CORRECTIONS (post-hoc)**: header stamps "v3.0.3-dirty" — run predated the
> v3.1.0 tag; rules under test = v3.1.0 candidate. Two harness defects were found and fixed
> DURING this series (both are harness bugs, not review failures):
> (1) the smoke run exposed a grader bug (decoy false positives were computed against the WHOLE
> report, not the text near the file mention) — fixed to ±300-char window matching before this
> series ran; run 3's D1 flag is a windowed, genuine-looking over-flag and is counted honestly.
> (2) the "fix task" line above reported fix-applied: NO / typecheck: FAIL because the harness
> ran headless WITHOUT edit permissions (the model could not write at all) and the typecheck
> invocation itself was broken (`npx typescript tsc` → correct `npx -p typescript tsc`; the
> fixture's JSX shim was also incomplete — pristine-baseline typecheck now verified PASS).
> The corrected fix-task result is in v3.2.0-outcome-fixonly.md, which supersedes that line.

> **POST-RUN AMENDMENT**: the planted V6 secret literal was reshaped
> (sk_live_… → PLANTED-LIVE-SECRET-…) for the same push-protection reason; V6
> grading keywords updated accordingly. The 18/18 recall series ran with the
> original literal; the semantic class (hardcoded credential in source) is
> identical and V6 detection cues in the reports were "hardcoded/secret/env",
> not the prefix.
