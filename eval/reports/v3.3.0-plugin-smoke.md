# Plugin runtime smoke — plugin under test: v3.2.0-dirty

- Step 1 `claude plugin validate plugin/`: **OK** (deterministic)
- Step 2 `--plugin-dir` runtime trigger: **2/2 runs fired** a plugin security component
  (project has NO rules installed — observed skills/agents can only come from the plugin;
  namespaced names below prove origin)
- Model: session default · prompt: src/api/users/route.ts のセキュリティチェックして

| run | observed (raw, namespaced) |
|---|---|
| 1 | agent:fable-frontend:security-reviewer, agent:general-purpose |
| 2 | agent:fable-frontend:security-reviewer |
