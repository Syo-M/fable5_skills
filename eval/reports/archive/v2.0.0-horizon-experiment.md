# Trigger evaluation — rules under test: v1.9.1-dirty (working tree; 3 run(s)/prompt)

- Scope: SUBSET run (--only security-form-jp,chart-en,react-state-jp): 3 of 29 prompts — NOT a full-series result
- Model: claude-sonnet-4-6 · max-turns 6 · fixture: Vite+React SPA
- Result: **3/3 prompts fully passed** (0 flaky, 0 failed)
- Method: headless `claude -p` in a disposable project installed via install.sh; activations measured
  by hooks (PostToolUse Skill/Task, SubagentStart, InstructionsLoaded) — observed, not self-reported.
- Run count: 3 (release-grade)

| id | lang | rate | expected | observed (union) | notes |
|---|---|---|---|---|---|
| security-form-jp | ja | 3/3 | skills:frontend-security | react-patterns, css-modules, frontend-security, rule:styling |  |
| chart-en | en | 3/3 | skills:data-viz | react-patterns, css-modules, data-viz, rule:server-boundaries, rule:styling |  |
| react-state-jp | ja | 3/3 | skills:react-patterns | react-patterns, rule:server-boundaries |  |

> **EXPERIMENT CONCLUSION (max-turns sensitivity)**: this is the falsification test named in
> v2.0.0-retest-3gaps.md, run at `--max-turns 6` (vs the series' 3) on the v2.0.0-candidate tree
> (git-describe `-dirty` — includes the forms.md tripwire and react-patterns/data-viz pointer
> edits). Result: ALL THREE former 0/3 gaps became 3/3 — `security-form-jp` loads
> `frontend-security`, `chart-en` loads `data-viz`, `react-state-jp` loads `react-patterns`,
> each within turns 4–6. The "loads later than the 3-turn measurement horizon" hypothesis is
> CONFIRMED for all three; "never loads" is refuted. Interpretation: at a realistic turn budget,
> every prompt in the golden set has demonstrated activation; the 3-turn series numbers (23/29,
> 85%/run) remain the conservative headline because turn-3 activation is what a user sees
> earliest, but the gaps are horizon artifacts, not missing triggers.
