# Trigger evaluation — rules under test: v2.1.0-dirty (working tree; 3 run(s)/prompt)

- Scope: SUBSET run (--only form-tripwire-jp): 1 of 34 prompts — NOT a full-series result
- Model: claude-sonnet-4-6 · max-turns 3 · fixture: Vite+React SPA · styling profile: css-modules
- Result: **1/1 prompts fully passed** (0 flaky, 0 failed)
- Method: headless `claude -p` in a disposable project installed via install.sh; activations measured
  by hooks (PostToolUse Skill/Task, SubagentStart, InstructionsLoaded) — observed, not self-reported.
- Run count: 3 (release-grade)

| id | lang | rate | expected | observed (union) | notes |
|---|---|---|---|---|---|
| form-tripwire-jp | ja | 3/3 | skills:frontend-security rules:forms | css-modules, react-patterns, frontend-security, new-component |  |

> **PROVENANCE + FINDING (post-hoc)**: SUBSET run (1 prompt), v3.0.0 candidate, default profile.
> The security goal is met — `frontend-security` loaded in 3/3 runs when creating ContactForm.tsx —
> but via the SKILL route; `rule:forms` itself has zero observed firings in every series to date.
> Mechanism hypothesis (docs-consistent): path rules inject when matching files are READ; creating
> a brand-new file doesn't traverse the read path, so the tripwire covers EDITS of existing form
> files, not first creation. The forms.md backstop claim is narrowed accordingly (see eval/README
> Backlog); first-creation coverage comes from the load-first directive + react-patterns pointer,
> which is what this measurement shows working.
